[project]
name = "query-engine"
version = "0.1.0"
description = "Structured query DSL engine"
requires-python = ">=3.12"
license = "MIT"
[project.dependencies]
pydantic = "^2.0"
sqlalchemy = "^2.0"
sqlalchemy-utils = "^0.39.0"
